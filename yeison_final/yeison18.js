let num13 = prompt("Ingrese un número YEISON DAVID MORENO NIETO:");
let suma = 0;
for (let i = 0; i < num13.length; i++) {
  suma += parseInt(num13[i]);
}
alert("La suma de los dígitos es: " + suma);
