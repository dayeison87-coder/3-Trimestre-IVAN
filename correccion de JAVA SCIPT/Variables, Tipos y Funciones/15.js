const areaYeison = (davidBase, davidAltura) => {
return (davidBase * davidAltura) / 2;
};

const areaYeisonCorto = (davidBase, davidAltura) =>
(davidBase * davidAltura) / 2;

console.log("Área:", areaYeison(10, 5));

// TIP: Puedes usar funciones flecha con return implícito para simplificar cálculos como este.